% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalty.R
\name{check.penalty}
\alias{check.penalty}
\title{Validate and resolve penalty settings}
\usage{
check.penalty(
  pen = NULL,
  pen.choice = NULL,
  p = NULL,
  c1 = NULL,
  c2 = NULL,
  mu = NULL,
  std = NULL
)
}
\arguments{
\item{pen}{Penalty name (character).}

\item{pen.choice}{Preset number or NULL.}

\item{p, c1, c2}{Beta penalty hyperparameters (used when pen.choice is NULL).}

\item{mu, std}{Normal penalty hyperparameters (used when pen.choice is NULL).}
}
\value{
A list with elements pen, p, c1, c2, mu, std.
}
\description{
Normalizes the penalty name (e.g., "Beta" to "beta") and, when
\code{pen.choice} is given, resolves the preset hyperparameters:
for \code{pen="beta"}, choices 1-6 give (p, c1, c2) =
(6,3,1), (6,5,2), (6,7,3), (2,3,0.5), (2,5,1), (2,7,1.5);
for \code{pen="norm"}, choices 1-4 give (mu, std) =
(-0.5,0.25), (-0.5,0.15), (-0.6,0.25), (-0.6,0.15).
These presets follow Table 1 and Eq. (15) of Shin et al. (2025).
}
\keyword{internal}
